apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infuxdb_secret_controller.fullname" . }}-test-connection"
  labels:
    {{- include "infuxdb_secret_controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infuxdb_secret_controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
