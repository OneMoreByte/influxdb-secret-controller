apiVersion: v1
kind: Service
metadata:
  name: {{ include "infuxdb_secret_controller.fullname" . }}
  labels:
    {{- include "infuxdb_secret_controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infuxdb_secret_controller.selectorLabels" . | nindent 4 }}
